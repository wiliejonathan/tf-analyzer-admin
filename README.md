# TF Analyzer Analyst — License Admin REV298

GitHub Pages static admin dashboard.

## Configure

Edit `config.js` and set `apiUrl` to the Apps Script Web App URL ending in `/exec`.

Do **not** place `ADMIN_DASHBOARD_KEY` in this repository. The admin key is entered manually in the login screen and kept only in browser memory while the page is open.
